services:
  - type: web
    name: qbits
    env: docker
    region: singapore
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false   # set this manually in Render dashboard
    healthCheckPath: /
